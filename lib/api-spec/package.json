{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),f='../../lib/api-zod/src/index.ts',c=fs.readFileSync(f,'utf8').replace(/export (?:type )?[*] from ['\\\"].+generated.types['\\\"];?\\n?/,'');fs.writeFileSync(f,c);\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.18.0"
  }
}
